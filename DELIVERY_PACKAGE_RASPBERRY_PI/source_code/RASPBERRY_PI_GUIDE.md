# 🍓 Руководство по установке на Raspberry Pi 4

## 📋 Требования

### Аппаратные требования
- **Raspberry Pi 4** (рекомендуется 4GB RAM или больше)
- **MicroSD карта** (32GB или больше, класс 10)
- **USB-кабель** для подключения к устройству
- **Сеть** (WiFi или Ethernet)

### Программные требования
- **Raspberry Pi OS** (64-bit рекомендуется)
- **Python 3.7+**
- **pip3**

## 🚀 Установка

### 1. Подготовка Raspberry Pi
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3 python3-pip -y

# Установка зависимостей
pip3 install flask pyserial pyinstaller
```

### 2. Настройка прав доступа к портам
```bash
# Добавление пользователя в группу dialout
sudo usermod -a -G dialout $USER

# Перезагрузка для применения изменений
sudo reboot
```

### 3. Проверка доступных портов
```bash
# Просмотр доступных USB портов
ls /dev/ttyUSB* /dev/ttyACM*

# Обычно используется:
# /dev/ttyUSB0 - для USB-to-Serial адаптеров
# /dev/ttyACM0 - для Arduino и подобных устройств
```

## 🔧 Настройка приложения

### Автоматическая сборка (рекомендуется)
```bash
# Сделать скрипт исполняемым
chmod +x build_raspberry.sh

# Запустить сборку
./build_raspberry.sh
```

### Ручная сборка
```bash
# 1. Создание защищенной сборки
python3 build_protected_raspberry.py

# 2. Переход в директорию сборки
cd protected_build_raspberry

# 3. Создание исполняемого файла
pyinstaller --onefile --noconsole app_raspberry.py
```

## 🎯 Запуск приложения

### Локальный запуск
```bash
# Запуск исполняемого файла
./protected_build_raspberry/dist/PWM_Generator_Pro

# Или запуск исходного кода
python3 app_raspberry.py
```

### Запуск в фоне
```bash
# Запуск в фоне
nohup ./PWM_Generator_Pro > app.log 2>&1 &

# Просмотр логов
tail -f app.log
```

### Автозапуск при загрузке
```bash
# Создание systemd сервиса
sudo nano /etc/systemd/system/pwm-generator.service
```

Содержимое файла сервиса:
```ini
[Unit]
Description=PWM Generator Application
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/pwm-generator
ExecStart=/home/pi/pwm-generator/PWM_Generator_Pro
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация сервиса:
```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска
sudo systemctl enable pwm-generator.service

# Запуск сервиса
sudo systemctl start pwm-generator.service

# Проверка статуса
sudo systemctl status pwm-generator.service
```

## 🌐 Доступ к приложению

### Локальный доступ
- **URL**: `http://localhost:5000`
- **URL**: `http://127.0.0.1:5000`

### Удаленный доступ
- **URL**: `http://[IP_АДРЕС_RASPBERRY_PI]:5000`
- **Пример**: `http://192.168.1.100:5000`

### Определение IP адреса
```bash
# Просмотр IP адреса
hostname -I

# Или
ip addr show
```

## 🔧 Настройка портов

### По умолчанию
Приложение настроено на использование порта `/dev/ttyUSB0`

### Изменение порта
1. Откройте веб-интерфейс
2. В поле "COM Port" введите нужный порт:
   - `/dev/ttyUSB0` - для USB-to-Serial адаптеров
   - `/dev/ttyACM0` - для Arduino
   - `/dev/serial0` - для встроенного UART

### Проверка подключения
```bash
# Проверка доступности порта
ls -la /dev/ttyUSB*

# Проверка прав доступа
groups $USER
```

## 🛠️ Устранение неполадок

### Проблема: "Permission denied" при доступе к порту
```bash
# Решение: добавление пользователя в группу dialout
sudo usermod -a -G dialout $USER
sudo reboot
```

### Проблема: Порт не найден
```bash
# Проверка подключенных устройств
lsusb

# Проверка доступных портов
ls /dev/tty*

# Проверка драйверов
dmesg | grep tty
```

### Проблема: Приложение не запускается
```bash
# Проверка зависимостей
pip3 list | grep -E "(flask|pyserial)"

# Проверка логов
cat app.log

# Запуск с отладкой
python3 app_raspberry.py
```

### Проблема: Не удается подключиться извне
```bash
# Проверка firewall
sudo ufw status

# Открытие порта (если нужно)
sudo ufw allow 5000

# Проверка сетевых подключений
netstat -tlnp | grep 5000
```

## 📊 Мониторинг

### Просмотр логов
```bash
# Логи приложения
tail -f app.log

# Системные логи
sudo journalctl -u pwm-generator.service -f
```

### Мониторинг ресурсов
```bash
# Использование CPU и памяти
htop

# Использование диска
df -h

# Температура Raspberry Pi
vcgencmd measure_temp
```

## 🔄 Обновление

### Обновление приложения
```bash
# Остановка сервиса
sudo systemctl stop pwm-generator.service

# Замена файла
cp new_PWM_Generator_Pro /home/pi/pwm-generator/

# Запуск сервиса
sudo systemctl start pwm-generator.service
```

## 📱 Мобильный доступ

### Настройка для мобильных устройств
1. Убедитесь, что Raspberry Pi и мобильное устройство в одной сети
2. Откройте браузер на мобильном устройстве
3. Перейдите по адресу: `http://[IP_RASPBERRY_PI]:5000`
4. Интерфейс автоматически адаптируется под мобильный экран

## 🎯 Оптимизация производительности

### Для Raspberry Pi 4
```bash
# Увеличение GPU памяти (если нужно)
sudo nano /boot/config.txt
# Добавить: gpu_mem=128

# Отключение неиспользуемых сервисов
sudo systemctl disable bluetooth
sudo systemctl disable wifi-powersave
```

### Мониторинг температуры
```bash
# Создание скрипта мониторинга
nano monitor_temp.sh
```

Содержимое скрипта:
```bash
#!/bin/bash
while true; do
    temp=$(vcgencmd measure_temp | cut -d= -f2)
    echo "$(date): $temp"
    sleep 60
done
```

---

**Примечание**: Данное руководство оптимизировано для Raspberry Pi 4 с Raspberry Pi OS. Для других версий могут потребоваться дополнительные настройки.
